# KametiPro — Frontend (React + Vite)

> Full documentation is in the root [`README.md`](../README.md)

## Quick Start

```bash
npm install
npm run dev
```

Runs at → **http://localhost:5173**

Make sure the backend is running at `http://localhost:5000` first.
